<%= bootstrap_form_for(@artifact, label_errors: true) do |f| %>
  <%= f.text_field :title,
			:required => true,
			:size=>"50",
      :readonly => true,
      placeholder: "This value will be auto-populated after uploading a file... 👇"
  %>
  <%= build_artifact_form(action_name,f) %>
  <%= f.submit "Save" %>
<% end %>
